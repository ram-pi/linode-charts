networks:
  monitoring:
    driver: bridge

volumes:
  prometheus_data: {}
  grafana_data: {}

services:
  exporter:
    build:
      context: .
      dockerfile: Dockerfile
    image: pylinode-vlan-topology-exporter:dev
    container_name: pylinode-vlan-topology-exporter
    restart: unless-stopped
    environment:
      - LINODE_TOKEN=${LINODE_TOKEN:?set LINODE_TOKEN in your shell}
      - SCRAPE_INTERVAL_SECONDS=60
      - MAX_WORKERS=32
      - MAX_RPS=15
      - METRICS_PORT=9108
      - VLAN_LABEL_FILTER=
    ports:
      - "9108:9108"
    networks:
      - monitoring

  topology-ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    image: pylinode-vlan-topology-ui:dev
    container_name: pylinode-vlan-topology-ui
    restart: unless-stopped
    environment:
      - TOPOLOGY_UI_PORT=9200
      - TOPOLOGY_METRICS_URL=http://exporter:9108/metrics
      - TOPOLOGY_REFRESH_SECONDS=60
      - TOPOLOGY_HTTP_TIMEOUT_SECONDS=5
    ports:
      - "9200:9200"
    networks:
      - monitoring
    depends_on:
      - exporter

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    restart: unless-stopped
    volumes:
      - prometheus_data:/prometheus
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    networks:
      - monitoring
    depends_on:
      - exporter
      - topology-ui

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_DISABLE_LOGIN_FORM=true
      - GF_UNIFIED_ALERTING_ENABLED=true
    ports:
      - "3000:3000"
    networks:
      - monitoring
    depends_on:
      - prometheus
